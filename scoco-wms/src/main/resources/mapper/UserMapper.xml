<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.scoco.wms.dao.UserDao" >

    <select id="login" parameterType="com.scoco.wms.fo.LoginUserFo" resultType="com.scoco.wms.entity.User">
        <![CDATA[
        SELECT u.* FROM t_user u WHERE status <> 'DELETE'
        AND expire_date> NOW()
        and password_expire_date> now()
        and (lock_date is null or lock_date < now())
        ]]>
         and user_name=#{userName,jdbcType=VARCHAR}
         and password=#{password,jdbcType=VARCHAR}
    </select>

</mapper>